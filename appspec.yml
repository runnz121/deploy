version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/deploy_test
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
#  ApplicationStop:
#    - location: scripts/stop.sh
#      timeout: 60
#      runas: ubuntu

  ApplicationStart:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu
